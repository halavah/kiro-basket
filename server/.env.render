# ================================================
# 生产环境配置
# ================================================
NODE_ENV=production
PORT=22300

# ================================================
# MySQL 数据库配置 (db4free.net)
# ================================================
MYSQL_HOST=db4free.net
MYSQL_PORT=3306
MYSQL_USER=kiro_basket
MYSQL_PASSWORD=UE6dzM9T*z6rQv
MYSQL_DATABASE=kiro_basket

# ================================================
# MongoDB 数据库配置 (MongoDB Atlas)
# ================================================
MONGODB_URI=mongodb+srv://troyesivens:Hg8dVG18BDFrBqW9@halavah.sbfgy.mongodb.net/kiro_basket_logs?retryWrites=true&w=majority&appName=halavah

# ================================================
# JWT 配置 (生产环境 - 必须使用强随机密钥)
# ================================================
# 🔒 重要：生产环境必须替换为随机生成的密钥！
# 生成命令：node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=CHANGE_THIS_TO_RANDOM_SECRET_IN_PRODUCTION
JWT_EXPIRES_IN=7d

# ================================================
# 其他配置
# ================================================
# 上传文件大小限制（MB）
MAX_FILE_SIZE=10

# API 前缀
API_PREFIX=/api

# ================================================
# 生产环境专用配置
# ================================================
# 是否启用详细日志（生产环境关闭）
DEBUG=false

# CORS 允许的来源（生产环境明确指定）
CORS_ORIGIN="https://kiro-basket-client-captain.onrender.com,https://kiro-basket-client-resident.onrender.com,https://kiro-basket-client-captain.halavah.us,https://kiro-basket-client-resident.halavah.us,https://kiro-basket-client-captain.troyesivens.workers.dev,https://kiro-basket-client-resident.troyesivens.workers.dev"

# ================================================
# 数据库初始化配置
# ================================================
# 是否强制重新初始化数据库（生产环境建议 false）
# true: 每次启动删除所有表并重建（所有数据丢失）⚠️ 危险！
# false: 智能检查，仅在必要时初始化（保留现有数据）
FORCE_DB_REINIT=false

# ================================================
# Render 部署配置
# ================================================
# Render Deploy Hook URL (用于触发重新部署)
# 获取方式: Render Dashboard > Service Settings > Deploy Hook
RENDER_DEPLOY_HOOK_SERVER=
