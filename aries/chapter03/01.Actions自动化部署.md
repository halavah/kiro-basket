# GitHub Actions 自动化部署指南

> 本文档介绍如何使用 GitHub Actions 实现自动化部署

## 📋 部署策略

### Monorepo 单仓库方案

本项目采用 **单仓库多项目（Monorepo）** 的部署策略：

```
kiro-basket/                    # 单个 Git 仓库
├── server/                     # 后端 → Render
├── client-captain/             # 团长端前端 → Vercel
├── client-resident/            # 居民端前端 → Vercel
└── .github/workflows/          # CI/CD 配置
    └── deploy.yml              # 自动部署工作流
```

### 优势

✅ **智能部署**：只部署有变更的项目，节省时间和资源
✅ **统一管理**：所有代码在一个仓库，便于版本管理
✅ **代码复用**：共享配置和工具脚本
✅ **简化协作**：单一代码库，协作更简单

---

## 1. 自动化部署工作流程

### 触发条件

以下操作会自动触发部署：

1. **推送代码到 master/main 分支**
   ```bash
   git push origin master
   ```

2. **创建 Pull Request**（预览部署）
   ```bash
   gh pr create
   ```

3. **手动触发**
   - 在 GitHub Actions 页面点击 "Run workflow"

### 智能检测

GitHub Actions 会自动检测哪些目录发生了变更：

```yaml
server/ 有变更      → 部署后端到 Render
client-captain/ 有变更  → 部署团长端到 Vercel
client-resident/ 有变更 → 部署居民端到 Vercel
```

### 部署流程

```
┌─────────────────────────────────────────────────┐
│  1. Push 代码到 GitHub                           │
└──────────────────┬──────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────┐
│  2. GitHub Actions 自动触发                      │
│     - 检测变更的目录                              │
│     - 运行相应的部署任务                          │
└──────────────────┬──────────────────────────────┘
                   │
        ┌──────────┼──────────┐
        ▼          ▼          ▼
   ┌────────┐ ┌────────┐ ┌────────┐
   │ Render │ │Vercel  │ │Vercel  │
   │ 后端   │ │团长端  │ │居民端  │
   └────────┘ └────────┘ └────────┘
        │          │          │
        └──────────┴──────────┘
                   │
                   ▼
        ✅ 部署完成，服务更新
```

---

## 2. 配置 GitHub Secrets

在使用自动化部署前，需要在 GitHub 仓库中配置 Secrets。

### 2.1 进入 Secrets 设置

1. 打开你的 GitHub 仓库
2. 点击 **Settings** → **Secrets and variables** → **Actions**
3. 点击 **New repository secret**

### 2.2 配置 Render Secrets

#### RENDER_DEPLOY_HOOK_URL

获取 Render Deploy Hook：

1. 登录 [Render Dashboard](https://dashboard.render.com)
2. 选择你的后端服务
3. 进入 **Settings** 标签
4. 滚动到 **Deploy Hook** 部分
5. 点击 **Create Deploy Hook**
6. 复制生成的 URL（格式：`https://api.render.com/deploy/srv-xxxxx?key=xxxxx`）
7. 在 GitHub 添加 Secret：
   - Name: `RENDER_DEPLOY_HOOK_URL`
   - Value: 粘贴复制的 URL

### 2.3 配置 Vercel Secrets

#### VERCEL_TOKEN

获取 Vercel Token：

1. 登录 [Vercel Dashboard](https://vercel.com/account/tokens)
2. 点击 **Create Token**
3. 设置 Token 名称（如 `GitHub Actions`）
4. 选择 Scope（建议选择具体的 Team 或 Account）
5. 点击 **Create**
6. 复制生成的 Token
7. 在 GitHub 添加 Secret：
   - Name: `VERCEL_TOKEN`
   - Value: 粘贴复制的 Token

#### VERCEL_ORG_ID

获取 Organization ID：

1. 在本地项目目录运行：
   ```bash
   cd client-captain  # 或 client-resident
   npx vercel link
   ```
2. 按提示登录并关联项目
3. 查看生成的 `.vercel/project.json`：
   ```bash
   cat .vercel/project.json
   ```
4. 复制 `orgId` 的值
5. 在 GitHub 添加 Secret：
   - Name: `VERCEL_ORG_ID`
   - Value: 粘贴 orgId

#### VERCEL_CAPTAIN_PROJECT_ID

团长端项目 ID：

1. 在 `client-captain/.vercel/project.json` 中找到 `projectId`
2. 在 GitHub 添加 Secret：
   - Name: `VERCEL_CAPTAIN_PROJECT_ID`
   - Value: 粘贴 projectId

#### VERCEL_RESIDENT_PROJECT_ID

居民端项目 ID：

1. 在 `client-resident/.vercel/project.json` 中找到 `projectId`
2. 在 GitHub 添加 Secret：
   - Name: `VERCEL_RESIDENT_PROJECT_ID`
   - Value: 粘贴 projectId

### 2.4 Secrets 清单

完成后，你应该有以下 Secrets：

| Secret Name | 用途 | 来源 |
|-------------|------|------|
| `RENDER_DEPLOY_HOOK_URL` | 触发 Render 部署 | Render Dashboard |
| `VERCEL_TOKEN` | Vercel CLI 认证 | Vercel Account Settings |
| `VERCEL_ORG_ID` | Vercel 组织 ID | `vercel link` |
| `VERCEL_CAPTAIN_PROJECT_ID` | 团长端项目 ID | `.vercel/project.json` |
| `VERCEL_RESIDENT_PROJECT_ID` | 居民端项目 ID | `.vercel/project.json` |

---

## 3. 使用自动化部署

### 3.1 首次部署

```bash
# 1. 确保所有代码已提交
git status

# 2. 推送到 GitHub
git add .
git commit -m "配置 GitHub Actions 自动部署"
git push origin master

# 3. 查看部署进度
# 访问：https://github.com/你的用户名/kiro-basket/actions
```

### 3.2 日常开发流程

```bash
# 1. 修改代码（例如修改后端）
cd server
# 修改文件...

# 2. 提交代码
git add .
git commit -m "修复后端 bug"
git push origin master

# 3. GitHub Actions 自动检测 server/ 目录变更
# 4. 只部署后端到 Render，前端不受影响
```

### 3.3 手动触发部署

如果需要手动部署：

1. 访问 GitHub Actions 页面：
   ```
   https://github.com/你的用户名/kiro-basket/actions
   ```

2. 点击左侧 **Deploy to Vercel and Render**

3. 点击右侧 **Run workflow**

4. 选择分支，点击 **Run workflow**

---

## 4. 查看部署状态

### 4.1 GitHub Actions 页面

查看实时部署日志：

1. 进入仓库的 **Actions** 标签
2. 点击最新的 workflow 运行
3. 查看每个 job 的详细日志

### 4.2 状态徽章

在 README.md 中添加部署状态徽章：

```markdown
![Deploy Status](https://github.com/你的用户名/kiro-basket/actions/workflows/deploy.yml/badge.svg)
```

### 4.3 部署结果

成功部署后：

- ✅ **后端**：Render 自动重启服务
- ✅ **团长端**：Vercel 更新并生成新的部署 URL
- ✅ **居民端**：Vercel 更新并生成新的部署 URL

---

## 5. 高级配置

### 5.1 分支策略

创建不同环境的部署：

```yaml
# .github/workflows/deploy-staging.yml
on:
  push:
    branches:
      - develop  # 开发分支自动部署到测试环境
```

### 5.2 添加测试步骤

在部署前运行测试：

```yaml
jobs:
  test:
    name: 运行测试
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: 运行后端测试
        run: |
          cd server
          npm install
          npm test
```

### 5.3 部署通知

添加部署通知到 Slack/Discord：

```yaml
- name: 发送通知
  uses: 8398a7/action-slack@v3
  with:
    status: ${{ job.status }}
    webhook_url: ${{ secrets.SLACK_WEBHOOK }}
```

### 5.4 回滚策略

如果部署失败，可以在 Render/Vercel Dashboard 手动回滚到上一个版本。

---

## 6. 故障排查

### 问题 6.1：Secrets 未配置

**症状**：
```
⚠️ 未配置 RENDER_DEPLOY_HOOK_URL，跳过部署
```

**解决**：
检查 GitHub Secrets 是否正确配置。

### 问题 6.2：Vercel 部署失败

**症状**：
```
Error: Failed to deploy to Vercel
```

**排查步骤**：
1. 检查 `VERCEL_TOKEN` 是否有效
2. 检查 `VERCEL_ORG_ID` 和 `VERCEL_PROJECT_ID` 是否正确
3. 本地运行 `vercel link` 重新关联项目

### 问题 6.3：Render 部署未触发

**症状**：
后端代码更新但 Render 没有重新部署

**解决**：
1. 检查 `RENDER_DEPLOY_HOOK_URL` 是否正确
2. 手动访问 Deploy Hook URL 测试：
   ```bash
   curl -X POST "你的Deploy Hook URL"
   ```

### 问题 6.4：权限错误

**症状**：
```
Error: Permission denied
```

**解决**：
- Vercel Token 需要有足够的权限
- 重新生成 Token 并确保 Scope 正确

---

## 7. 最佳实践

### 7.1 环境变量管理

生产环境的敏感信息不要放在代码中：

```bash
# ❌ 错误：在代码中硬编码
const apiUrl = 'https://api.example.com';

# ✅ 正确：使用环境变量
const apiUrl = import.meta.env.VITE_API_BASE_URL;
```

在 Render 和 Vercel Dashboard 中配置环境变量，不要提交到 Git。

### 7.2 分支保护

为 master 分支添加保护规则：

1. GitHub 仓库 → **Settings** → **Branches**
2. 添加规则：
   - ✅ Require pull request reviews
   - ✅ Require status checks to pass

### 7.3 代码审查

使用 Pull Request 流程：

```bash
# 1. 创建功能分支
git checkout -b feature/new-feature

# 2. 开发和提交
git add .
git commit -m "添加新功能"
git push origin feature/new-feature

# 3. 创建 Pull Request
# 4. 审查通过后合并到 master
# 5. 自动触发生产环境部署
```

### 7.4 版本标记

为重要版本打标签：

```bash
git tag -a v1.0.0 -m "Version 1.0.0"
git push origin v1.0.0
```

---

## 8. 部署检查清单

在推送代码前检查：

- [ ] 代码已在本地测试通过
- [ ] 所有必需的环境变量已配置
- [ ] GitHub Secrets 已正确设置
- [ ] commit message 清晰描述改动
- [ ] 如果修改了数据库结构，已更新相关文档

---

## 9. 与手动部署的对比

| 特性 | 手动部署 | 自动化部署 (GitHub Actions) |
|------|---------|---------------------------|
| 部署速度 | 慢（需要人工操作） | 快（自动触发） |
| 一致性 | 可能遗漏步骤 | 每次都一致 |
| 错误率 | 较高 | 较低 |
| 可追溯性 | 难以追踪 | 完整日志 |
| 团队协作 | 需要共享账号 | 基于 Git，无需共享 |
| 回滚 | 手动操作 | 可自动回滚 |

---

## 10. 配置总结

### 需要配置的文件

```
kiro-basket/
├── .github/workflows/
│   └── deploy.yml              # ✅ 已创建
├── server/
│   └── .env                    # 本地开发用
├── client-captain/
│   ├── .env.production         # 生产环境变量
│   └── .vercel/
│       └── project.json        # Vercel 项目配置
└── client-resident/
    ├── .env.production         # 生产环境变量
    └── .vercel/
        └── project.json        # Vercel 项目配置
```

### 需要配置的 Secrets

在 GitHub 仓库设置中添加 5 个 Secrets（见第 2 节）。

---

## 11. 下一步

1. ✅ 配置所有 GitHub Secrets
2. ✅ 推送代码触发首次自动部署
3. ✅ 验证部署是否成功
4. ✅ 测试修改不同目录是否只部署相应项目

---

## 附录：快速参考

### 常用命令

```bash
# 查看 Vercel 项目信息
vercel ls

# 手动部署到 Vercel
vercel --prod

# 查看 Render 服务状态
curl https://你的服务.onrender.com/health

# 触发 Render 部署
curl -X POST "$RENDER_DEPLOY_HOOK_URL"

# 查看 Git 提交历史
git log --oneline --graph --all

# 创建并推送标签
git tag v1.0.0
git push origin v1.0.0
```

### 重要链接

- GitHub Actions 文档: https://docs.github.com/actions
- Vercel CLI 文档: https://vercel.com/docs/cli
- Render Deploy Hooks: https://render.com/docs/deploy-hooks

---

**🎉 配置完成后，你只需要 `git push`，剩下的交给 GitHub Actions！**
